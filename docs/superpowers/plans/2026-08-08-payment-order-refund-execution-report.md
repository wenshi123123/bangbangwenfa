# 支付、订单、续费与退款整改执行报告

## 目标

让咨询、律师入驻、律师续费和退款流程保持同一套规则：订单不串单，未支付订单 15 分钟后关闭，支付成功后订单/会员/通知一致，退款只允许在支付后 24 小时内申请。

## 已实现

1. 咨询订单唯一业务键改为“用户 + 民事/刑事 + 案件类型 + 三种套餐（基础/标准/高级）”。不同案件或套餐不会复用错误订单；同一业务键仍只保留一笔有效未支付订单。
2. 订单中心对 `pending` 和 `paying` 均显示继续支付入口；支付中订单复用原支付地址，过期订单关闭后可重新下单。
3. 律师续费按具体卡种隔离：民事季卡、民事年卡、刑事季卡、刑事年卡互不阻塞；民事律师可以购买刑事续费，反之亦然。
4. 续费回调按民事/刑事分别维护会员记录，总览到期日取有效会员的最晚日期；重复回调不会重复延长。
5. 支付回调优先读取微信独立签名头，兼容旧 Authorization 头；数据库更新失败返回失败，允许微信重试；首次支付成功才发送企业微信通知。
6. 用户端和管理员端均校验 `paid_at + 24 小时` 退款窗口；续费退款成功后撤销本次订单产生的会员记录，并重新计算剩余会员权益。
7. 修复后台律师支付状态把 `paid` 错映射为“待支付”的问题。

## 数据库迁移

新增 `supabase/migrations/20260807120000_consult_order_identity.sql`：

- 增加 `consult_orders.plan_id`；
- 增加支付地址字段；
- 删除旧的“用户+分类”有效订单索引；
- 建立“用户+分类+案件类型+套餐”部分唯一索引；
- 关闭没有支付截止时间且超过 15 分钟的历史未支付订单。

## 验证结果

- TypeScript：通过。
- 生产构建：通过，Next.js 生成 104 个页面，服务器打包成功。
- 针对订单生命周期、续费退款 UI、支付回调和迁移契约测试：通过。
- 全量历史契约测试中有若干与本次范围无关的既有失败，已单独记录，未修改这些业务：守护者身份契约、移动端按钮结构、静态资源预热契约、律师入驻文案契约。

## 发布状态

- 提交：`7a5bc51 fix: harden payment orders renewals and refunds`
- 已推送到 GitHub `main`。
- CloudBase 当前在线版本为 `bangbangwenfa-018`，流量比例 100%，构建状态正常。
- 构建日志确认 `.next/static`、`public` 已复制到 runner，生产构建和服务器打包成功。
- Supabase 迁移 `20260808045447_consult_order_identity` 已执行成功；线上已存在新的部分唯一索引和 `plan_id` 字段。
- 公网首页与 CloudRun 默认域名的 HTML 静态资源引用均已逐个检查，16 个 `_next/static` 资源全部返回 200。
- `x-bbwv-deployment-id` 仍显示静态资源发布标识 `20260721-cache-consistency-201000`，它不是 CloudBase 版本号；CloudBase 版本以 018 部署记录为准。

## 上线后的验收顺序

1. 已确认 CloudBase 018 构建成功并切换 100% 流量。
2. 已执行并确认数据库迁移成功。
3. 待使用已登录测试账号创建不同案件/不同咨询套餐，确认订单互不串联。
4. 待验证 15 分钟未支付订单关闭，以及订单中心继续支付入口。
5. 待用真实律师账号购买跨类型续费，检查两类会员到期时间。
6. 待用真实小额支付验证订单、支付时间、企业微信通知和会员权益同时更新。
7. 待验证支付后 24 小时内可申请退款，超过 24 小时用户端和管理员端均拒绝。

## 回滚条件

若出现支付成功但订单/会员未更新、重复通知、跨案件串单、退款越过 24 小时或回调签名失败，立即将 CloudBase 流量回退到上一正常部署，并保留失败订单号和回调日志。
