import Link from 'next/link'; import { notFound } from 'next/navigation';
export default function AdminPreview(){ if(process.env.NODE_ENV==='production'||process.env.DEPLOY_ENV==='PROD') notFound(); return <main className="mx-auto max-w-5xl space-y-5 bg-slate-50 px-5 py-10"><Link href="/local-preview" className="text-sm text-blue-700">← 返回页面目录</Link><p className="text-xs text-amber-700">本地样稿，仅供 UI 预览</p><h1 className="text-3xl font-bold text-slate-900">管理后台</h1><section className="grid gap-4 sm:grid-cols-3">{[['待审核律师','6'],['今日订单','18'],['待处理提现','2']].map(([a,b])=><div key={a} className="rounded-xl bg-white p-5 shadow-sm"><p className="text-sm text-slate-500">{a}</p><p className="mt-2 text-3xl font-bold">{b}</p></div>)}</section><section className="rounded-xl bg-white p-6 shadow-sm"><h2 className="text-xl font-bold">律师入驻审核</h2><p className="mt-2 text-slate-600">本地样稿律师 · 民事律师（臻选） · 资料待审核</p><div className="mt-4 flex gap-3"><button className="rounded-lg bg-blue-600 px-4 py-2 text-white">通过审核</button><button className="rounded-lg border px-4 py-2">查看资料</button></div></section></main> }
